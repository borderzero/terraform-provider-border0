// ===========================================================================
// Create a EC2 instance there and leverage user_data_base64 to install the
// connector and configure it with the connector token created from this
// terraform example.
// ===========================================================================

